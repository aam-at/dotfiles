# Hyprland Configuration
# https://wiki.hypr.land/Configuring/
#
# Desktop-shell profile entrypoint.  The generated local selector defaults to
# Noctalia; use `desktop-shell dms` to switch both Hyprland and Niri together.
source = ./shells/active.conf
